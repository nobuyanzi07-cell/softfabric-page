let firstName='John'
let secondName='GPT'
let age=75
let address='Jupiter'

let userDetails=firstName+ " " +
secondName+" is "+age+" years old "+ "he stays in "+address
console.log(userDetails)
console.log ("user details is",typeof userDetails)

let userDetails2=`${firstName} ${secondName} is ${age} years old and stays at ${address}`;
console.log (userDetails2)

