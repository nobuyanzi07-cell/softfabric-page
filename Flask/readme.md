intro to flask

Creating a flask app; requirments
1. have pipenv
2. python version 3.0
3. ensure in bash you have line
    export PIPENV_VENV_IN_PROJECT=1
    ensures the created virtual enviroment will be in the project root

Starting point
1. Create project folder
2. create virtual enviroment
